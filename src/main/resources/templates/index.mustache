{{>layout/header}}
<br/>
<div id="post_list">
    <table id="table" class="table table-horizontal">
        <thead id="thead">
        <tr class="text-center">
            <th>번호</th>
            <th class="col-5">제목</th>
            <th>작성자</th>
            <th>작성일</th>
            <th>조회수</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#post}}
            <tr class="bd-highlight">
                <td class="text-center">{{id}}</td>
                <td>
                    <span class="align-text-top bi bi-chat-dots" style="font-size: small;">{{commentsCount}}</span>
                    <a href="/post/read/{{id}}" id="title">{{title}}</a>
                </td>
                <td class="text-center">{{writer}}</td>
                <td class="text-center">{{timeStamp}}</td>
                <td class="text-center">{{view}}</td>
            </tr>
        {{/post}}
        </tbody>
    </table>
    <div class="text-end ">
        <a href="/post/write" role="button" class="btn btn-primary bi bi-pencil-fill"> 글쓰기</a>
    </div>
</div>
{{>layout/footer}}