{{! Comments }}
<div class="card">
    <div class="card-header bi bi-chat-dots"> {{#comments.size}}{{comments.size}}{{/comments.size}} Comments</div>
    {{! 댓글내용 부분 }}
    <ul class="list-group-flush">
        {{#comments}}
            <li id="comments-{{id}}" class="list-group-item">
                <span>
                    <span style="font-size: small">{{writer}}</span>
                    <span style="font-size: xx-small">{{timeStamp}}</span>
                </span>
                {{#isWriter}}
                    <a type="button" onclick="deleteComment({{id}})"
                       class="bi bi-x-square"></a> {{! 댓글 삭제 버튼 }}
                {{/isWriter}}

                {{! 댓글 내용 보기 }}
                <p class="pt-2">{{contents}}
                    <span class="badge badge-primary badge-pill">3</span>
                </p>

            </li>
        {{/comments}}
    </ul>
</div>